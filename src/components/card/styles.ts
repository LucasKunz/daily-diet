import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    alignItems: "center",
    borderRadius: 8,
    gap: 8,
    padding: 16,
    width: "auto",
  },
  description: {},
  title: {
    fontSize: 24,
    fontWeight: "700",
  },
});
